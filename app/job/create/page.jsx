import CreateForm from "./CreateForm";

export default function CreateProject() {
  return (
    <main>
        <h2 className="text-primary">
            Add new project
        </h2>
        <CreateForm/>
    </main>
  )
}